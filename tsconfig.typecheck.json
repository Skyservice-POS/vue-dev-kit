{
  "extends": "./tsconfig.json",
  "include": [
    "src/**/*.ts",
    "src/**/*.d.ts",
    "src/**/*.vue"
  ],
  "//": "src/vue2 і src/compat типізуються проти Vue 2, який у цьому репозиторії стоїть під аліасом `vue2`. vue-tsc резолвить `vue` на Vue 3 і на цих шарах дає хибні помилки — перевіряти їх треба своїм прогоном тестів, а не тут.",
  "exclude": [
    "node_modules",
    "dist",
    "src/vue2",
    "src/compat"
  ]
}
