{
  "extends": "./tsconfig.json",
  "include": ["src/**/*.ts", "src/**/*.d.ts", "src/**/*.vue"],
  "//": "src/vue2 типізується проти Vue 2, який у цьому репозиторії стоїть під аліасом `vue2`. vue-tsc резолвить `vue` на Vue 3 і на цьому шарі дає хибні помилки — перевіряти його треба своїм прогоном тестів, а не тут.",
  "exclude": ["node_modules", "dist", "src/vue2"]
}
